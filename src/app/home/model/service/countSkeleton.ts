export const countSkeleton = (): number => {

	// const widthPageOfDocument = document.body.getClientRects()[0].width;
	// if (widthPageOfDocument < 640) return 4;
	// if (widthPageOfDocument < 1024) return 6;

	return 8;
};
