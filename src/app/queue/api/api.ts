// import { rtkApi } from '@/shared/api/rtkApi';
// import { Trackes } from '@/shared/api';

// export const queueApi = rtkApi.injectEndpoints({
// 	endpoints(build) {
// 		return {
// 			fetchQueue: build.query<Trackes, void>({
// 				query: () => ({
// 					url: '/api/track',

// 				}),
// 			}),
// 		}
// 	},
// }
